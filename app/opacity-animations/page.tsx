import React from 'react'
import OpacityAnimation from '../components/opacity-animations/OpacityAnimation'
import Opacity from '../components/opacity-animations/Opacity'

interface Props { }

function Page(props: Props) {
    const { } = props

    return (
        <div>
            <Opacity />
        </div>
    )
}

export default Page
