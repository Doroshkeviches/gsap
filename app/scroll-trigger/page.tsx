import React from 'react'
import ScrollTriggerDemo from '../components/scroll-trigger/ScrollTrigger'

interface Props { }

function Page(props: Props) {
    const { } = props

    return (
        <ScrollTriggerDemo />
    )
}

export default Page
