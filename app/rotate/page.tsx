import React from 'react'
import Rotate from '../components/rotate/Rotate'

interface Props { }

function Page(props: Props) {
    const { } = props

    return (
        <Rotate />
    )
}

export default Page
