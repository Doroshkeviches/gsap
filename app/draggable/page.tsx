import React from 'react'
import Weather from '../components/draggable/weather/Weather'

interface Props { }

function Page(props: Props) {
    const { } = props

    return (
        <Weather />
    )
}

export default Page
